version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongodb_task_api
    restart: always
    ports:
      - "27017:27017"
      
    volumes:
      - ./mongo-data:/data/db
volumes:
  mongo-data: